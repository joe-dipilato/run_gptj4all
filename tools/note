added_test = []
removed_test = []
new_pass = []
new_fail = []

# [{test.name : test for test in suite} for suite in xml_prev]

# for testsuite in xml_prev:
#     for test in testsuite:
#         if test.name not in xml_curr.tests:
#             removed_test.append(test)
# for testsuite in xml_curr:
#     for test in testsuite:
#         if test.name not in xml_prev.tests:
#             added_test.append(test)
#         elif test.result != xml_prev.tests[test.name].result:
#             if test.result == 'passed':
#                 new_pass.append(test)
#             else:
#                 new_fail.append(test)

# print(f"{added_test=}")
# print(f"{removed_test=}")
# print(f"{new_pass=}")
# print(f"{new_fail=}")


